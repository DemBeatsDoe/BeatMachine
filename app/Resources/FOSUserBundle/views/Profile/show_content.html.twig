{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    {#<p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>#}

    <h3 class="text-center">Your Password is successfully changed
        <br>
        <small>Please log out and sign in to the system again using new password</small>
    </h3>
    <div class="col-xs-5 col-md-3"></div>
    <div class="col-xs-8 col-md-6">
        <br>
        <a href="{{ logout_url() }}" class="btn btn-success btn-block" role="button">Log out</a>
    </div>
    <div class="col-xs-5 col-md-3"></div>





</div>
